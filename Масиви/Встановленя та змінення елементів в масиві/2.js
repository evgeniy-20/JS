// Створіть масив із елементами 'a', 'b', 'c'. Запишіть замість першого елемента число 1замість другого - 2замість третього - 3.

let arr = ['a', 'b', 'c'];
arr[0] = 1;
arr[1] = 2;
arr[2] = 3;

console.log(arr);
