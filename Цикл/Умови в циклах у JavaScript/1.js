// Дано такий масив: Виведіть у консоль ті елементи масиву, які більші за 3, але менші за 10.

let arr = [2, 5, 9, 15, 1, 4];

for (let elem of arr) {
    if (elem > 3 && elem < 10) {
        console.log(elem);
    }
}
