// Визначте, скільки цілих чисел, починаючи з числа 1, потрібно скласти, щоб сума вийшла більше 100.
let sum = 0;
let count = 0;

for (let i = 1; sum <= 100; i++) {
    sum += i;
    count++;
}

console.log('Кількість цілих чисел, які потрібно скласти, щоб сума вийшла більше 100:', count);
