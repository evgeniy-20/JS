// Складіть масив днів тижня. За допомогою циклу for виведіть усі дні тижня, а вихідні виведіть жирним.

let daysOfWeek = ['Понеділок', 'Вівторок', 'Середа', 'Четвер', 'П`ятниця', 'Субота', 'Неділя'];

for (let i = 0; i < daysOfWeek.length; i++) {
    if (i === 5 || i === 6) {
        console.log(daysOfWeek[i].bold()); // Вихідні дні виводяться жирним шрифтом
    } else {
        console.log(daysOfWeek[i]);
    }
}
