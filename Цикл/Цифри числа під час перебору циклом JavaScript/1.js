// Переберіть циклом числа від 10 до 1000 і виведіть у консоль першу цифру кожного числа.
for (let i = 10; i <= 1000; i++) {
    let str = String(i);
    if (str[0] === '1') {
        console.log(str[0]);
    }
}
