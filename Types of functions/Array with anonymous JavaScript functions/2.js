// Використовуючи створений вами масив, arr виведіть в консоль число 3, викликавши відповідну функцію.

let arr = [
    function () { return 1 },
    function () { return 2 },
    function () { return 3 },
];

console.log(arr[2]);
