// Зробіть функцію test, параметрами приймаючої 3функції. Передайте в неї першим параметром функцію, що повертає 1, другим – функцію, що повертає 2, третім – функцію, що повертає 3. Виведіть у консоль суму результатів функцій.
test(
    function () { return 1; },
    function () { return 2; },
    function () { return 3; }
);

function test(func1, func2, func3) {
    console.log(func1() + func2() + func3());
}
