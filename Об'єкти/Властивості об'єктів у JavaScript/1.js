// Створіть об'єкт date з ключами 'year', 'month' і 'day' і значеннями, що відповідають поточному дню. Виведіть створену дату на екран у форматі рік-місяць-день.

let date = {
    'year': 2024,
    'month': 2,
    'day': 22
}

let result = date.year + '-' + date.month + '-' + date.day;

console.log(result);
