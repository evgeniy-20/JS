// Дано абзаци: Перевірте для змінної elems описані можливості псевдомасивів.

let elems = document.querySelectorAll('p');
console.log(elems.length);
for (let elem of elems) {
    console.log(elem)
}
