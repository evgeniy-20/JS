// Завдання: Створіть кілька символів з описами

let userId = Symbol('ідентифікатор користувача');
let orderId = Symbol('ідентифікатор замовлення');
let sessionToken = Symbol('токен сесії');

console.log(userId);      // Symbol(ідентифікатор користувача)
console.log(orderId);     // Symbol(ідентифікатор замовлезамовлення
