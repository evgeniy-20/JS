// Є Set з числами. Виведи кожне число в консолі через цикл.
let nums = new Set([10, 20, 30]);
for (let elem of nums) {
    console.log(elem);
}
// 👉 Очікування: виведе 10, 20, 30
