// Нехай у змінній num зберігається число. Визначте, парне число чи ні. Число буде парним, якщо останній символ дорівнює 0, 2, 4, 6 або 8, і непарним в іншому випадку.

let num = 46;

if (num % 2 === 0) {
    console.log('Число парне');
} else {
    console.log('Число непарне')
}
