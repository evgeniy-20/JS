// Автор наведеного нижче коду хотів виконати перевірку віку на досягнення 18 років. Після перевірки коду виявилося, що якщо вік дорівнює або більший за 18 років, то до змінної adult записується true, як і повинно бути, однак, якщо вік менший за 18, то змінна adult має значення undefined. Виправте помилку автора коду. Ось проблемний код:

let age = 17;
let adult;

if (age >= 18) {
    adult = true;
} else {
    adult = false;
}

console.log(adult);
