// Дано число, наприклад, 12345. Перевірте, що першим символом цього числа є цифра 1, 2 або 3. Якщо це так - виведіть 'так', в іншому випадку виведіть 'ні'.

let num = 12345;
let str = String(num);

if (str[0] === '1' || str[0] === '2' || str[0] === '3') {
    console.log('Yes');
} else {
    console.log('No');
}
