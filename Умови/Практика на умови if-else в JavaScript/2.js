// Дано рядок, що складається із символів, наприклад, 'abcde'. Перевірте, що першим символом цього рядка є буква 'a'. Якщо це так - виведіть 'так', в іншому випадку виведіть 'ні'.

let str = 'absde';

if (str[0] === 'a') {
    console.log('Yes');
} else {
    console.log('No');
}
