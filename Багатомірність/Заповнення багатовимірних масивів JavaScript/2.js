// Сформуйте за допомогою двох вкладених циклів такий масив:
// [
// 	['x', 'x', 'x', 'x', 'x'],
// 	['x', 'x', 'x', 'x', 'x'],
// 	['x', 'x', 'x', 'x', 'x']
// ]
let arr = [];

for (let i = 0; i < 3; i++) {
    arr[i] = []; // Створюємо підмасив
    for (let j = 0; j < 5; j++) {
        arr[i].push('x'); // Додаємо символ 'x' до підмасиву
    }
}

console.log(arr);
