// Даний наступний масив: Виведіть з його допомогою елементи з текстом 'l', 'e', 'g'та 'a'.

let arr = [
    ['a', 'b', 'c'],
    ['d', 'e', 'f'],
    ['g', 'h', 'i'],
    ['j', 'k', 'l'],
];

let result = arr[3][2] + arr[1][1] + arr[2][0] + arr[0][0];

console.log(result);
