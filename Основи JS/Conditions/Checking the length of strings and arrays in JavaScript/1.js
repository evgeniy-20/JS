// У змінній arr міститься деякий масив із числами. Напишіть умову, яка перевірить, що в масиві 3елемента. Якщо так, виведіть на екран суму елементів масиву.

let arr = [1,2,3];

let sum = 0;

if(arr.length === 3) {
    console.log(sum += arr);
}