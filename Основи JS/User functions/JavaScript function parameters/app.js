//Зробіть функцію, яка параметром приймає число та виводить у консоль куб цього числа.

const getCube = (num) => {
    return Math.pow(num, 3);
}
console.log(getCube(5));

//Зробіть функцію, яка параметром приймає число і перевіряє, чи позитивне число, чи негативне. У першому випадку нехай функція виводить у консоль текст '+++', а у другому '---'.
const getCheck = (num1) => {
   if(num1 > 0) {
     return '+++';
   } else {
    return '---';
   }
}
console.log(getCheck(-5));