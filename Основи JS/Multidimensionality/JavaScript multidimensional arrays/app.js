//Даний наступний масив:  Виведіть з його допомогою елементи з текстом 'l', 'e', 'g'та 'a'.

let arr = [
	['a', 'b', 'c'],
	['d', 'e', 'f'],
	['g', 'h', 'i'],
	['j', 'k', 'l'],
];
let str = arr[3][2] + arr[1][1] + arr[2][0] + arr[0][0];
console.log(str);
//Даний наступний масив: Звертаючись до кожного елемента масиву, знайдіть суму всіх його елементів.
let arr1 = [[1, 2], [3, 4], [5, 6]];
let sum = 0;
for(let i = 0; i < arr1.length; i++) {
    sum += arr1[i];
}
console.log(sum);