// Даний наступний масив: let arr = [2, 5, 9, 15, 1, 4]; Виведіть в консоль ті елементи масиву, які більше 3-х, але менше 10.

let arr = [2, 5, 9, 15, 1, 4];
for(let elem of arr) {
    if(elem > 3 && elem < 10) {
        console.log(elem);
    }
}