// Даний код:За допомогою описаного прийому з плюсом виправте наведений код так, щоб у змінні a і bприсвоювалося число, а не рядок, та й результатом, відповідно, було не '23', а 5.

let a = '2';
let b = '3';
console.log(+a + +b);
