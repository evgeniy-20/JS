// Знайдіть кількість секунд у 30 добі.

let secondsInMinutes = 60;
let minutesInHour = 60;
let hoursInDay = 24;
let secondsInDay = secondsInMinutes * minutesInHour * hoursInDay;
let days = 30;
let secondsInDays = secondsInDay * days;
console.log(secondsInDays);
