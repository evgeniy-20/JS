// Даний наступний масив: Виведіть в консоль ті елементи масиву, які більше 3-х, але менше 10.

let arr = [2, 5, 9, 15, 1, 4];

for (let i = 0; i < arr.length; i++) {
    if (arr[i] > 3 && arr[i] < 10) {
        console.log(arr[i]);
    }
}
