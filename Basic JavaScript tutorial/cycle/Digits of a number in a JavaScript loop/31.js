// Переберіть циклом числа від 10 до 1000 і виведіть числа, перша цифра яких дорівнює 1.

for (let i = 10; i <= 1000; i++) {
    let str = String(i);
    if (str[0] === '1') {
        console.log(str);
    }
}
