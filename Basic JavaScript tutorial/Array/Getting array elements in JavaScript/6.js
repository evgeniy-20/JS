// Даний наступний масив:Виведіть за допомогою цього масиву наступний рядок: 'a+b+c+d'

let arr = ['a', 'b', 'c', 'd'];

let str = arr[0] + '+' + arr[1] + '+' + arr[2] + '+' + arr[3];
console.log(str);
