// Дано масив із числами. Використовуючи метод map, витягніть із кожного елемента масиву квадратний корінь і запишіть результат у новий масив.

let arr = [1, 2, 3, 4, 5];
let newArray = arr.map((item) => Math.sqrt(item));
console.log(newArray);
