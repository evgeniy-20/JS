// Дано число, наприклад, 12345. Перевірте, що першим символом цього числа є цифра 1, 2 або 3. Якщо це так - виведіть 'да', в іншому випадку виведіть 'нет'

let num = 12345;
let first = String(num);
if (first[0] == 1 || first[0] == 2 || first[0] == 3) {
    console.log('Yes');
} else {
    console.log('No');
}
