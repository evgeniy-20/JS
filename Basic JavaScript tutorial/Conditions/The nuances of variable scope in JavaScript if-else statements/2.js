// Автор наведеного нижче коду хотів виконати перевірку віку для досягнення 18 років. Після перевірки коду виявилося, що якщо вік дорівнює або більше 18 років, то змінну adult записується true, як і має бути, проте, якщо вік менше 18, то змінна adult має значення undefined. Виправте помилку автора коду. Ось проблемний код:

let age = 27;
let adult;

if (age >= 18) {
    adult = true;
} else {
    adult = false;
}

console.log(adult);
