// Автор наведеного нижче коду хотів виконати перевірку віку для досягнення 18 років. Код, однак, не працює. Виправте помилку автора коду. Ось проблемний код:

let age = 27;
let adult;

if (age >= 18) {
    adult = true;
} else {
    adult = false;
}

console.log(adult);
