// Дано число 12345. Знайдіть добуток цифр цього числа.

let num = String(12345);

let multiplication = Number(num[0]) * Number(num[1]) * Number(num[2]) * Number(num[3]) * Number(num[4]);
console.log(multiplication);
