// Код повинен перевірити, чи є в масиві число 3 чи ні:

let arr = [1, 2, 3, 4, 5];
let res = '---'; // Спочатку вважаємо, що числа 3 немає

for (let elem of arr) {
    if (elem === 3) {
        res = '+++';
        break; // Виходимо з циклу, бо знайшли число 3
    }
}

console.log(res);
