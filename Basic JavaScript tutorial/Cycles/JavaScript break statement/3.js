// Даний масив із числами. Знайдіть позицію першого числа 3 у цьому масиві (вважаємо, що це число обов'язково є у масиві).

let arr = [1, 2, 3, 4, 5, 6, 7, 8, 9];
let index = arr.indexOf(3);
console.log(index);
