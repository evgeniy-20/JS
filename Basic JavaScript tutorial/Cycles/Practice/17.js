// Даний масив із числами. За допомогою циклу for та функції document.writ eвиведіть кожен елемент масиву з нового рядка. Використовуйте для цього тег br.

let arr = [1, 2, 3, 4, 5];
for (let i = 0; i < arr.length; i++) {
    document.write(arr[i] + '<br>')
}
