// Даний масив із числами. За допомогою циклу виведіть на екран усі елементи, значення яких збігаються з їх порядковим номером у масиві.
let arr = [0, 2, 2, 3, 5, 10];
for (let i = 0; i < arr.length; i++) {
    if (arr[i] === i) {
        console.log(arr[i]);
    }
}
