// Даний масив із числами. Перевірте те, що хоча б для одного елемента добуток його значення на порядковий номер більше 30.

let arr = [1, 2, 3, 4, 5, 6, 7];

let newArr = arr.some((elem, index) => elem * index > 30);

console.log(newArr);
