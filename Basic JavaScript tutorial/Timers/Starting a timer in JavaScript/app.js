// Виведіть у консоль число 0. Через секунду виведіть число 1, через дві секунди виведіть число 2, через 3 секунди виведіть число 3. І так далі до нескінченності.

let i = 0;
let timerId = setInterval(() => {
    console.log(i++);
    if (i > 10) {
        clearInterval(timerId);
    }
}, 1000);
