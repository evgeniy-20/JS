// Створіть об'єкт date із ключами 'year' та значеннями, що відповідають поточному дню 'month'. 'day' Виведіть створену дату на екран у форматі рік-місяць-день .

let date = {
    year: 2025,
    month: 2,
    day: 20
}

console.log(date.year + '-' + date.month + '-' + date.day);
