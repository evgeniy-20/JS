// Нехай функція, що передається, тепер приймає два параметри і повертає їх суму. При виклику функції, що передається всередині, test передайте в передавану функцію число 2 і число 3. Виведіть алертом результат.

let func = function (num1, num2) {
    return num1 + num2;
};

function test(func) {
    console.log(func(2, 3));
}

test(func);
