// Зробіть функцію, яка параметром приймає число, а повертає квадратний корінь із цього числа. За допомогою цієї функції знайдіть корінь числа 3, потім знайдіть корінь числа 4. Підсумуйте отримані результати та виведіть їх у консоль.

function getSqrt(num) {
    return Math.sqrt(num);
}
let res = getSqrt(3) + getSqrt(4);
console.log(res);
