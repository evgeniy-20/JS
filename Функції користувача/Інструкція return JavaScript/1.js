// Зробіть функцію, яка параметром приймає число, а повертає куб цього числа. За допомогою цієї функції знайдіть куб числа 3 і запишіть його у змінну res.

function getCube(num) {
    return Math.pow(num, 3);
}

let res = getCube(3);

console.log(res);
