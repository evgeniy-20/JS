// Вік Якщо вік користувача від 18 до 60 років (включно), виведіть "Доступ дозволено", інакше — "Доступ заборонено".

let num1 = 18;
let num2 = 60;
if (num1 >= 18 && num2 <= 60) {
    console.log("Доступ дозволено");
} else {
    console.log("Доступ заборонено");
}
