// Логін та пароль Реалізуйте перевірку: якщо логін "admin", а пароль "1234", виведіть "Вхід дозволено", інакше — "Невірні дані".

let login = "admin";
let password = "1234";
if (login === "admin" && password === '1234') {
    console.log("Вхід дозволено");
} else {
    console.log("Невірні дані");
}
