// Створи новий масив, де кожен елемент буде квадратом початкового.

let arr = [2, 4, 6];

// За даопомогою цикла проходимо по всьому масиву

for (let elem of arr) {
    Math.pow(elem, 2); // возводимо кожен елемент масиву в квадрат
}
