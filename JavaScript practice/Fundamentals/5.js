// Парне чи непарне Напиши програму, яка визначає, чи є число (змінна number) парним або непарним, і виводить відповідний текст у консоль.

let number = 4;

if (number % 2 === 0) {
    console.log('Парне!');
} else {
    console.log('Непарне!');
}
